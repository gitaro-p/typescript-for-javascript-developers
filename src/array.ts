export {};

let numbers: number[] = [1, 2, 3];
let numbers2: Array<number> = [1, 2, 3];

let strings: string[] = [' TypeScript', 'javaScript', 'coffeeScript'];

let nijigenHairetsu: number[][] = [
  [50, 100],
  [150, 300]
];

let hairetss: (string | number | boolean)[] = [1, false, 'japan'];

//array型は配列を格納するデータ型。
